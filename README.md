# 拉霸抽獎機 🎰

純靜態的拉霸機風格抽獎網站。貼上參加者名單、設定獎項，拉下拉桿開獎。

## 功能

- 貼上名單（一行一個名字，支援從 FB 留言直接複製）
- 設定多個獎項，每個獎項可指定中獎人數
- 拉霸機風格動畫開獎
- 已中獎的人不會重複中獎
- 中獎名單可一鍵複製

## 使用方式

直接用瀏覽器打開 `index.html` 即可使用，不需要任何伺服器。

## 技術

純 HTML + CSS + JavaScript，零依賴。
